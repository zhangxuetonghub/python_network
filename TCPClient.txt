伪代码
cs = socket() 
cs.connect()
com_loop:
  cs.send()/recv()
cs.close()

所有套接字都是socket.socket()创建的。然而，一旦客户端拥有了一个套接字，它就可以利用套接字的connect（）方法直接创建一个到服务
器的连接。当连接建立之后，他就可以参与到与服务器的一个对话中。最后，一旦客户端完成了它的事物，它就可以关闭套接字，终止连接。
