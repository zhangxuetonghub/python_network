UDP服务器不需要TCP服务器那么多的设置，因为它们不是面向连接的。除了等待传入的连接之外，几乎不需要做其他工作。
ss = socket()
ss.bind()
inf_loop:
  cs = ss.recvfrom()/ss.sendto()
ss.close()
除了创建套接字并将其绑定到本地地址外，并没有额外的工作。无线循环包含接收客户端消息、打上时间戳并返回消息，然后回到等待另一条消息的状态。
